// Keep in sync with the Rust-side constants in src-tauri/src/commands.rs
// (MIN_REFRESH_MINUTES / MAX_REFRESH_MINUTES / DEFAULT_REFRESH_MINUTES).
export const MIN_REFRESH_MINUTES = 1
export const MAX_REFRESH_MINUTES = 20
export const DEFAULT_REFRESH_MINUTES = 5
